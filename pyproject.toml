[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "web-scraper-portal-de-fraude"
version = "0.1.0"
description = "Python web scraping project structure"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
